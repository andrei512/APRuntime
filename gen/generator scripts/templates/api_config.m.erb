<%
=begin

APIConfig Implementation Template

prefix - class prefix

=end

%>
<%= header_for_file "#{prefix}APIConfig.m" %>

#import "<%= "#{prefix}APIConfig.h" %>"

@implementation <%= "#{prefix}APIConfig" %>

+ (instancetype)shared {
	static <%= "#{prefix}APIConfig" %> *shared = nil;
	if (shared == nil) {
		shared = [<%= "#{prefix}APIConfig" %> new];
	}
	return shared;
}

- (NSString *)apiKey {
	return @"<%= api_key %>";
}

- (NSString *)serverBaseUrl {
	return @"<%= server_base_url %>";
}

@end